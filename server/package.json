{
  "name": "bitscraft-server",
  "version": "1.0.0",
  "description": "Bits Craft",
  "main": "../dist/main.js",
  "scripts": {
    "test": "cd .. && npx jest --testPathPattern=server",
    "build": "cd .. && npx jest --testPathPattern=server && cd ./server && tsc",
    "dev": "tsc && cd ./dist/src && node server.js"
  },
  "author": "CM",
  "license": "MIT",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13"
  }
}
